<div class="container">
	<div class="row">
		<div class="col s12 l8 m8 offset-l2">
			<div class="card">
				<div class="card-content black-text">
					<span class="card-title">Registring Business info</span>
					<%= form_for(@business) do |f|%>
						<%= render(:partial => "form", :locals => {:f => f}) %>
	            		<div class="acton">
	              			<%= f.button "Submit", class: "btn center"%>
	            		</div>
	            	<%end%>
				</div>
			</div>
		</div>
	</div>
</div>
